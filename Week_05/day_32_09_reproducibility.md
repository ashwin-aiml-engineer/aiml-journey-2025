Reproducibility checklist (10–15 minutes)

- Pin dependencies (`requirements.txt` or `conda.yml`)
- Record random seeds and environment variables
- Snapshot training data (hash + storage location)
- Record Git commit hash and any config files
- Use Docker for full environment reproducibility

Mini task
- Create a minimal `requirements.txt` snippet that pins numpy and scikit-learn versions.
