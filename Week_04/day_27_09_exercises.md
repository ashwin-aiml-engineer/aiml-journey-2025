# Day 27 — 12 Quick Exercises (15-min each)

1. Run `day_27_02_basic_interface.py` and enter 5 example texts. Add examples to the interface.
2. Convert the basic interface to use a transformers sentiment pipeline (if installed).
3. Create an image upload demo with `day_27_05_cv_demos.py` and test with local images.
4. Build a Blocks layout with tabs for NLP and CV (extend `day_27_06_blocks_api.py`).
5. Add a progress bar and loading state to a heavy inference demo (simulate delay with time.sleep).
6. Create a multi-model interface that lets user choose model via a dropdown and runs selected model.
7. Add authentication to a demo by wrapping Gradio in a simple FastAPI reverse proxy (outline).
8. Create an audio demo that records a short clip and returns its duration.
9. Add client-facing branding: title, description, logo and example inputs.
10. Deploy a Gradio demo to Hugging Face Spaces (prepare `requirements.txt` and `README.md`).
11. Implement error handling for invalid inputs and show friendly messages to users.
12. Measure demo cold-start time and suggest two ways to reduce it.

Use these as focused 15-minute practice blocks for client demos.